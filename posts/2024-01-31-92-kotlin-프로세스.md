---
title: "Kotlin / 프로세스"
source: "https://dev-ej2.tistory.com/92"
tistory_id: "92"
published: "2024-01-31T01:53:54+09:00"
tags:
---
**<https://school.programmers.co.kr/learn/courses/30/lessons/42587>**

[프로그래머스

코드 중심의 개발자 채용. 스택 기반의 포지션 매칭. 프로그래머스의 개발자 맞춤형 프로필을 등록하고, 나와 기술 궁합이 잘 맞는 기업들을 매칭 받으세요.

programmers.co.kr](https://school.programmers.co.kr/learn/courses/30/lessons/42587)

**문제 설명**

운영체제의 역할 중 하나는 컴퓨터 시스템의 자원을 효율적으로 관리하는 것입니다. 이 문제에서는 운영체제가 다음 규칙에 따라 프로세스를 관리할 경우 특정 프로세스가 몇 번째로 실행되는지 알아내면 됩니다.

1. 실행 대기 큐(Queue)에서 대기중인 프로세스 하나를 꺼냅니다.

2. 큐에 대기중인 프로세스 중 우선순위가 더 높은 프로세스가 있다면 방금 꺼낸 프로세스를 다시 큐에 넣습니다.

3. 만약 그런 프로세스가 없다면 방금 꺼낸 프로세스를 실행합니다.

  3.1 한 번 실행한 프로세스는 다시 큐에 넣지 않고 그대로 종료됩니다.

예를 들어 프로세스 4개 [A, B, C, D]가 순서대로 실행 대기 큐에 들어있고, 우선순위가 [2, 1, 3, 2]라면 [C, D, A, B] 순으로 실행하게 됩니다.

현재 실행 대기 큐(Queue)에 있는 프로세스의 중요도가 순서대로 담긴 배열 priorities와, 몇 번째로 실행되는지 알고싶은 프로세스의 위치를 알려주는 location이 매개변수로 주어질 때, 해당 프로세스가 몇 번째로 실행되는지 return 하도록 solution 함수를 작성해주세요.

제한사항

- priorities의 길이는 1 이상 100 이하입니다.
- location은 0 이상 (대기 큐에 있는 프로세스 수 - 1) 이하의 값을 가집니다.

입출력 예

|  |  |  |
| --- | --- | --- |
| priorities | location | return |
| [2, 1, 3, 2] | 2 | 1 |
| [1, 1, 9, 1, 1, 1] | 0 | 5 |

입출력 예 설명

예제 #1

문제에 나온 예와 같습니다.

예제 #2

6개의 프로세스 [A, B, C, D, E, F]가 대기 큐에 있고 중요도가 [1, 1, 9, 1, 1, 1] 이므로 [C, D, E, F, A, B] 순으로 실행됩니다. 따라서 A는 5번째로 실행됩니다.

내풀이

```
class Solution {
    fun solution(priorities: IntArray, location: Int): Int {
        var answer = 0
        val tempList = mutableListOf<Int>()

        while (tempList.size < priorities.size) {
            for (i in priorities.indices) {
                if (priorities.maxOrNull() == priorities[i]) {
                    tempList.add(i)
                    priorities[i] = 0
                }

                if (tempList.size == priorities.size) {
                    break
                }
            }

            if (tempList.size == priorities.size) {
                break
            }
        }

        answer = tempList.indexOf(location) + 1

        return answer
        }
}
```

kotlin dequeue를 활용한 풀이

```
import java.util.*
class Solution {
    fun solution(priorities: IntArray, location: Int): Int {
            var printerQueue = ArrayDeque<Pair<Int,Int>>()
        priorities.forEachIndexed{index, i ->
            printerQueue.offer(Pair(index,i))
        }

        var count = 0
        while (!printerQueue.isEmpty()){
            var first = printerQueue.poll()

            if(printerQueue.filter { first.second < it.second }.size > 0){
                printerQueue.offer(first)
            }else{
                count++
                if(first.first == location) return count
            }
        }
        return 0
    }
}
```

ArrayDeque는 Kotlin에서 제공하는 덱(deque) 자료구조를 구현한 클래스이다.

덱은 큐(queue)와 스택(stack)의 특징을 모두 가지고 있어 앞과 뒤에서 모두 원소를 추가하거나 제거할 수 있다.

### poll 함수

- poll: 덱에서 요소를 제거하고 반환합니다. 만약 덱이 비어있다면 null을 반환한다.

### offer 함수

- offer: 덱의 끝에 요소를 추가합니다. 성공하면 true를 반환하고, 공간이 부족한 경우 false를 반환한다.

### add 함수

- add: offer와 동일한 기능을 수행한다. 요소를 덱의 끝에 추가한다. 만약 공간이 부족한 경우 IllegalStateException을 던진다.

### remove 함수

- remove: 덱에서 요소를 제거한다. 만약 덱이 비어있다면 NoSuchElementException을 던진다.

### 차이점

1. **반환 값의 유무:**
   - poll은 요소를 제거하고 반환하는데, 비어있으면 null을 반환한다.
   - offer와 add는 반환 값이 있으나, 성공적으로 추가했는지 여부를 나타낸다.
2. **예외 처리:**
   - add는 공간이 부족한 경우 IllegalStateException을 던진다.
   - remove는 비어있는 경우 NoSuchElementException을 던진다.
   - offer는 실패할 경우 false를 반환하여 예외를 발생시키지 않는다.
3. **사용 목적:**
   - poll과 offer는 주로 큐와 함께 사용된다.
   - add와 remove는 큐와 스택 양쪽 끝에서 모두 요소를 추가하거나 제거할 수 있어서 덱 자료구조의 특징을 활용할 수 있다.
